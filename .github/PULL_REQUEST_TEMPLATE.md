## What changed

## Why

## Tests
- [ ] Tests added or updated for the changed behaviour
- [ ] `pytest tests/` passes locally
- [ ] `black --check ictonyx/ tests/ --line-length 100` and `isort --check-only` pass
